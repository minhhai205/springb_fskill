server:
  port: 8082
spring:
  config:
    activate:
      on-profile: prod
open:
  api:
    title: API document
    version: v1.0.0
    description: API Service Description
    serverUrl: http://localhost:${server.port}
    serverName: server prod